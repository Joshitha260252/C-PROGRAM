#include <stdio.h>
void main()
{
   int a,b,r;
printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    int big=a>b?a:b;
    int small=a<b?a:b;
    while(b!=0){
        r=a%b;
        a=b;
        b=r;
    }
    printf("GCD is %d",a);                    
}

