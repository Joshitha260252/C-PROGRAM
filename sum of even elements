#include<stdio.h>
void main(){
    int a[]={1,2,3,4,5,6,7,8,9};
    int len=sizeof(a)/sizeof(a[0]);
    int sum=0;
    for (int i = 0; i < len; i++)
    {
       if(a[i]%2==0){
            sum=sum+a[i];
       }
    }
    printf("Sum of Even Elements in Given Array : %d",sum);
}
